This demo shows the following:

--Quick setup of AWS DateWarehouse
--Uses AWS Redshift 
    --Uses VPC & Security Groups
    --Uses IAM
    --Uses awscli
--Uses AWS Marketplace partners
    --Uses Matillion ETL
    --Uses Yellofin (data viz)

Questions:
--Which PostgreSQL client, if any? Navicat
--Souce data, use airline data? put in this repo or public S3 bucket?
--Try to use AWS CloudFormation?